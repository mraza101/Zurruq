import tailwindcss from '@tailwindcss/vite';
import react from '@vitejs/plugin-react';
import path from 'path';
import {defineConfig, loadEnv} from 'vite';

export default defineConfig(({mode}) => {
  const env = loadEnv(mode, '.', '');
  return {
    plugins: [react(), tailwindcss()],
    define: {
      'process.env.GEMINI_API_KEY': JSON.stringify(env.GEMINI_API_KEY),
    },
    resolve: {
      alias: {
        '@': path.resolve(__dirname, '.'),
      },
    },
    build: {
      rollupOptions: {
        input: {
          main: path.resolve(__dirname, 'index.html'),
          about: path.resolve(__dirname, 'about.html'),
          cocoa: path.resolve(__dirname, 'cocoa.html'),
          coffee: path.resolve(__dirname, 'coffee.html'),
          contact: path.resolve(__dirname, 'contact.html'),
          logistics: path.resolve(__dirname, 'logistics.html'),
          qaPack: path.resolve(__dirname, 'qa-pack.html'),
          sourcing: path.resolve(__dirname, 'sourcing.html'),
          wholesale: path.resolve(__dirname, 'wholesale.html'),
        },
      },
    },
    server: {
      // HMR is disabled in AI Studio via DISABLE_HMR env var.
      // Do not modifyâfile watching is disabled to prevent flickering during agent edits.
      hmr: process.env.DISABLE_HMR !== 'true',
    },
  };
});
